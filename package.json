{
  "name": "pagic-docs",
  "version": "0.0.1",
  "description": "theme for pagic based on docs",
  "main": "mod.ts",
  "scripts": {
    "lint": "deno lint --unstable --ignore=node_modules",
    "fmt": "deno fmt --check --unstable --ignore=node_modules",
    "fmt:fix": "deno fmt --unstable --ignore=node_modules"
  }
}
